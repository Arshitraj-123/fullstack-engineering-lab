{
  "name": "dyno-load-test",
  "version": "1.0.0",
  "description": "A load testing tool (configurable concurrency, ramp-up, live percentile metrics via SSE) run against a small demo target API, with a dynamometer test-cell themed UI.",
  "main": "server.js",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "jest --runInBand"
  },
  "dependencies": {
    "express": "^4.19.2",
    "morgan": "^1.10.0",
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.0",
    "supertest": "^6.3.4"
  }
}
